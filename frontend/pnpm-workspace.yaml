packages:
  - '.'
# Autorise les build scripts des paquets à binaire natif (vite/vitest/tailwind).
# Sans ça, un `pnpm install` neuf (CI) les ignore et le build/les tests cassent.
allowBuilds:
  '@tailwindcss/oxide': true
  core-js: true
  esbuild: true
